Array.from(document.querySelector('#colors-dropdown').options).forEach((option_element) => {
    let option_text = option_element.option_text.value;
    let option_value = option_element.option_value;
    let is_option_selected = option_element.value;


})